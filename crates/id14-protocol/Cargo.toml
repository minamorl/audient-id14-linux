[package]
name = "id14-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Pure, host-independent control-protocol library for the Audient iD14 (mk1/mk2)"

[dependencies]
